<%= form_with(model: organisation) do |form| %>
  <% if organisation.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(organisation.errors.count, "error") %> prohibited this organisation from being saved:</h2>

      <ul>
        <% organisation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="govuk-form-group">
    <%= form.label :name, style: "display: block", class: "govuk-label" %>
    <%= form.text_field :name, class: "govuk-input" %>
  </div>

  <div>
    <%= form.submit class: "govuk-button", data: { module: "govuk-button" } %>
  </div>
<% end %>
